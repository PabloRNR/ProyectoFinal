<div class="container" style="margin-top: 50px;">
    <div class="row">
        <div class="col">
            {{#if usuario}}
            <p class="text-end">Hola {{usuario}}!<a class="btn btn-warning" href="/admin/login/logout">Cerrar sesión <i
                        class="fas fa-sign-out-alt">
                    </i>
                </a>
            </p>
            {{/if}}
        </div>
    </div>
</div>
        <div class="row">
            <div class="col-7">
                <h2{{title}}</h2>
            </div>
        </div>
</div>
<div class="row">
    <div class="col">
        <table class="table">
            <tbody>
                <tr>
                    <td>    {{!-- aca va ir la imagen de la pelicula --}}

                    </td>
                    <td>    {{!-- aca va ir la imagen de la pelicula --}}
                        <tr></tr>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>